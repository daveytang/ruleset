# 静态 hosts 记录：直接返回指定 IP，不查询任何上游
#
# 优先级最高，排在所有规则最前面。
#
# 格式：域名 在前，IP 在后，用空格分隔；一个域名可以写多个 IP。
#   ✓ nas.internal 192.168.1.10
#   ✓ api.internal 10.0.0.5 10.0.0.6
#   ✓ v6.internal 2001:db8::1
#
# 注意本文件的格式和其他规则表相反：这里【必须】有空格，其他表【不能】有空格。
localhost 127.0.0.1
