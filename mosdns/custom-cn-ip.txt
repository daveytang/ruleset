# 视为「国内」的额外 IP 段
#
# 只在处理「不在任何域名表里的未知域名」时生效：先问国内 DNS，
# 若返回的 IP 落在 GeoIP 的 cn 段或本表内，就采纳这个结果；否则改问国外 DNS。
#
# 什么时候加到这里：
#   - 你自己的机房或专线 IP 段，希望未知域名解析到它时被当作国内结果接受
#
# 格式：一行一个 IP 或 CIDR。
#   ✓ 203.0.113.7
#   ✓ 203.0.113.0/24
#   ✓ 2001:db8::/32
#   ✗ 203.0.113.0/24 我的机房   （行尾不能写备注，同步会被拒绝并指出行号）
