# 拦截域名：直接返回 NXDOMAIN，不向任何上游查询
#
# GeoSite 的广告表（17.3 万条）已经自动生效，这里只写它没覆盖的。
#
# 格式：一行一个域名，自动匹配所有子域名。
#   ✓ ads.example.com
#   ✗ ads.example.com # 备注   （行尾不能写注释，同步会被拒绝）
#
# 本表在缓存之前处理，优先级高于所有分流规则。
# 误加会导致对应域名完全无法解析，加之前想清楚。
